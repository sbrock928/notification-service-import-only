# PR 4 — Power Automate Teams

Add `providers/power_automate/teams.py`. Map logical destination names to validated HTTPS webhook
configuration, post a bounded JSON contract, and reject unknown destinations before HTTP I/O.

Cover authentication headers, payload shape, acceptance IDs, throttling, server errors, and external
HTTP-client ownership with a mock transport.
