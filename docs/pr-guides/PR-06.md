# PR 6 — integration documentation and release gates

Export the supported public API, retain lightweight compatibility imports for the previous module
paths, and document construction for both initial transports and future Graph replacements.

Automated tests must require no Microsoft account, Windows host, Outlook process, or network access.
Treat live Outlook, Power Automate, and Graph sends as separate deployment gates.
