# PR 5 — Microsoft Graph migration adapters

Add `providers/microsoft_graph` with a shared token port and HTTP behavior. Implement email and Teams
providers using the existing domain models. Preserve logical Teams destination mapping so moving from
Power Automate to Graph does not change callers.

Keep Azure Identity optional and import it only when `ClientSecretToken` is constructed.
