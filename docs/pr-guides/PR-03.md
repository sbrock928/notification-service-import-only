# PR 3 — Outlook desktop email

Add `providers/win32com/outlook.py`. Initialize COM lazily in its worker thread, create the Outlook
mail item from `EmailNotification`, serialize calls, and normalize errors without returning COM
objects. Treat an exception during `MailItem.Send()` as ambiguous.

Add the platform-specific `outlook-win32` package extra and test with an injected fake application.
