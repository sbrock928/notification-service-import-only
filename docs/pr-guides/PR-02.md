# PR 2 — application orchestration

Create `notification_service/application` with a generic `NotificationProvider` protocol and
`NotificationClient`. Centralize conservative retry, timeout, delivery-state, policy, lifecycle, and
idempotency behavior so every provider follows the same semantics.

Keep shared idempotency behind `IdempotencyStore`; the included implementation is process-local.
