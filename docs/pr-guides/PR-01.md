# PR 1 — domain contracts

Create `notification_service/domain` with stable exceptions, provider outcomes, and immutable
`EmailNotification` and `TeamsNotification` models. A Teams caller supplies a logical destination;
URLs and Graph channel IDs never enter the domain model.

Review that importing the package performs no I/O and domain files import no infrastructure module.
